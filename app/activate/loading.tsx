import { PublicFormSkeleton } from "@/components/ui/loading-skeletons";

export default function ActivationLoading(): React.JSX.Element {
  return <PublicFormSkeleton label="กำลังโหลดหน้าเปิดใช้งานบัญชี..." />;
}
